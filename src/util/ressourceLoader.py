import pygame

class RessurceLoader():
	def __init__(self):
		self.graphicDict = {}
 	def load_graphic(self,path):
		if not path in self.graphicDict:
 			#TODO: fill in util.util code
			self.graphicDict[path] = pygame.image.
		return self.graphicDict[path]
			